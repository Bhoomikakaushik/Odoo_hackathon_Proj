import express from "express";
import mongoose from "mongoose"
const app = express();

// app.set("port",(process.env.PORT || 8000))


// app.get("/",(req, res) => {
//     return "odoooo"
// })

// app.listen(port,(req,res) => {
//     return res.json(`app is listening on port ${port}`)
// })